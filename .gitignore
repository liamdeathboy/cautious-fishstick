# GTA Vice City WASM dev project (Vite source, node config, screenshots, zip).
# The playable game is self-hosted at games/gta-vice-city/ — a trimmed static
# copy with the full runtime asset set (core .data split into <50 MB parts, plus
# the vcsky/ radio + cutscene + model streams). This dev folder is reference
# only — keep it local, don't deploy it.
games/GTA-Vice-City-Online-Play-Free-in-Browser-main/

# Staging library of scraped games (518 deduped folders, ~9 GB) plus its shared
# EmulatorJS runtime. Source material only — the games that ship are copied into
# games/ individually. Keep it local, never commit it.
ugb new games/
